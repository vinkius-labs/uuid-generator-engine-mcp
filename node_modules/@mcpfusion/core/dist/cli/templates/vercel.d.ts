/**
 * Vercel Templates — Next.js App Router scaffold for @mcpfusion/vercel
 *
 * Generates a complete Next.js project that exposes an MCP server
 * via `vercelAdapter()`. Deploy with `vercel deploy`.
 *
 * @module
 */
import type { ProjectConfig } from '../types.js';
/** Generate `package.json` for Vercel/Next.js project */
export declare function vercelPackageJson(config: ProjectConfig): string;
/** Generate `tsconfig.json` for Next.js */
export declare function vercelTsconfig(): string;
/** Generate `next.config.ts` */
export declare function vercelNextConfig(): string;
/** Generate `app/api/mcp/route.ts` — The MCP endpoint */
export declare function vercelRouteTs(config: ProjectConfig): string;
/** Generate `src/mcp/registry.ts` — Tool registry (cold-start) */
export declare function vercelRegistryTs(): string;
/** Generate `src/mcp/mcpfusion.ts` — initMCPFusion instance */
export declare function vercelFusionTs(): string;
/** Generate `src/mcp/context.ts` — Application context */
export declare function vercelContextTs(): string;
/** Generate `.env.example` for Vercel */
export declare function vercelEnvExample(config: ProjectConfig): string;
/** Generate `.gitignore` for Next.js */
export declare function vercelGitignore(): string;
/** Generate `README.md` for Vercel project */
export declare function vercelReadme(config: ProjectConfig): string;
//# sourceMappingURL=vercel.d.ts.map